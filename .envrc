# shellcheck shell=bash
# Nix dev shell: provides zig, swiftlint, xcodes, zls.
# Swift toolchain comes from system Xcode (not Nix).

if has use_flake; then
  use flake
fi

watch_file flake.nix
watch_file flake.lock
